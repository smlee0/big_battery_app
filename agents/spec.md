# 프로젝트 명세서: Big Battery Widget App

## 🧭 프로젝트 개요
**프로젝트명:** `big_battery_widget_app`  
**플랫폼:** Flutter 3.x  
**목적:** 부모님 세대가 보기 쉽게, **큰 글씨와 단순한 디자인으로 배터리 잔량을 표시하는 홈 화면 위젯 앱**

---

## 🧱 핵심 기능
1. **배터리 잔량 표시**
    - 현재 배터리 퍼센트(%)를 크게 표시
    - 배터리 수준에 따라 색상 변경 (초록: 50%↑, 노랑: 20~50%, 빨강: 20%↓)
    - 충전 중이면 번개 아이콘 표시

2. **홈 위젯 지원**
    - 안드로이드 홈화면에서 바로 배터리 확인 가능
    - 2x2, 4x2 크기 위젯 지원
    - 일정 주기 혹은 배터리 변화 시 자동 갱신

3. **접근성 강화**
    - 큰 글씨 + 높은 명암 대비
    - 설정에서 “고대비 모드” ON/OFF 가능

4. **설정 화면**
    - 글씨 크기 선택 (크게 / 아주 크게)
    - 테마 색상 선택 (기본 / 다크)
    - 위젯 갱신 주기 설정 (수동 / 자동)

5. **알림 기능 (선택)**
    - 배터리 20% 이하 시 “충전이 필요해요!” 알림 표시
    - 충전 중 여부를 알림에 함께 표시

---

## 🧩 기술 스택
| 구성 요소 | 선택 | 설명 |
|------------|------|------|
| 프레임워크 | Flutter 3.x | 크로스 플랫폼 지원 |
| 언어 | Dart | Flutter 기본 언어 |
| 상태관리 | Provider | 간단하고 가벼운 상태관리 |
| 배터리 정보 | `battery_plus` 패키지 | 배터리 퍼센트 및 충전 상태 조회 |
| 위젯 API | Flutter AppWidget / Glance | 안드로이드 위젯용 |
| 로컬저장 | `shared_preferences` | 사용자 설정 저장 |
| 알림 | `flutter_local_notifications` | 배터리 알림 기능 |

---

## 🧰 폴더 구조
```
lib/
 ├── main.dart
 ├── app.dart
 ├── screens/
 │    ├── home_screen.dart
 │    ├── settings_screen.dart
 ├── widgets/
 │    ├── battery_display.dart
 │    ├── battery_widget.dart
 ├── services/
 │    ├── battery_service.dart
 │    ├── notification_service.dart
 ├── providers/
 │    └── battery_provider.dart
 └── utils/
      └── theme.dart
```

---

## 🎨 디자인 가이드
- **메인 화면:** 중앙에 배터리 % 크게 표시 + 충전 아이콘
- **컬러 팔레트:**
    - 메인 블루 `#6FA8DC`
    - 라이트 배경 `#F8F9FB`
    - 회색 텍스트 `#C8CDD6`
    - 빨강 경고 `#E74C3C`
- **폰트:** Noto Sans / Pretendard / Roboto

---

## 🧠 동작 방식
- 앱 실행 시 즉시 배터리 잔량 표시
- `BatteryState` 스트림 구독하여 실시간 업데이트
- 20% 이하 → 로컬 알림 트리거
- 위젯이 등록된 경우 자동 갱신

---

## 📦 빌드 및 실행
1. 프로젝트 초기화
   ```bash
   flutter create big_battery_widget_app
   cd big_battery_widget_app
   flutter pub add battery_plus provider shared_preferences flutter_local_notifications
   ```
2. 실행
   ```bash
   flutter run
   ```
3. 릴리즈 빌드
   ```bash
   flutter build apk --release
   ```

---

## 🪄 Codex 작업 목표
Codex가 다음을 자동 생성해야 함:
- Flutter 기본 구조 및 파일 생성
- `battery_plus` 연동
- `Provider` 기반 상태관리 (`BatteryProvider`)
- `home_screen.dart`, `battery_widget.dart` 구현
- 글자 크기와 테마 변경 가능한 설정화면 포함

---

## 🧩 결과물 기대 형태
- `lib/main.dart`: 앱 진입점
- `lib/screens/home_screen.dart`: 배터리 상태 실시간 표시
- `lib/widgets/battery_display.dart`: 시각적 퍼센트/아이콘
- `lib/providers/battery_provider.dart`: 상태관리
- `pubspec.yaml`: 필요한 패키지 포함

---

## 🧾 네이밍 규칙
| 항목 | 규칙 |
|------|------|
| 패키지명 | `big_battery_widget_app` |
| 루트 클래스 | `BigBatteryWidgetApp` |
| 메인 화면 | `BatteryWidgetScreen` |
| 상태관리 클래스 | `BatteryProvider` |

---

🎯 **프롬프트 목적:**  
Codex가 위 명세를 기반으로 Flutter 앱 초기 구조를 자동 생성하도록 한다.  
필요한 패키지, 화면, 상태관리, 위젯 구조를 모두 포함해야 한다.
